define([
    'namespace',
    'underscore',
     './home',
     './transitions',
     './regions'
],

function(ns, _) {
	var app = ns.app;

    // Required, return the module for AMD compliance
    return;
});